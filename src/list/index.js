import React, { Component } from 'react';

export default class Input extends Component {
    state = {
        image: '',
    };

    render() {
        return (
            <div>
                
            </div>
        );
    }
}